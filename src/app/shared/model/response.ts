/**
 * Model describes a resource technical information, 
 * example: when posting a resource to the server for creation, the backend respond with this model
 */
export interface ResourceInformation {
    id: number;
}
