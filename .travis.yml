language: node_js
node_js:
  - "10"
dist: trusty
sudo: required
branches:
  only:
  - develop
before_script:
  - npm install -g @angular/cli
script:
  - ng lint
  - ng build --prod
deploy:
  skip_cleanup: true
  provider: firebase
  token:
    secure: ""